var active_menu = $('.active').attr('href').replace(/\/users\/[0-9]+/g, '');
switch(active_menu){
  case "/followings":
  $(".jscroll-inner").html("<%= j(render partial: "users/users", locals: {users: @referer_user.followings.order('relationships.created_at DESC')}) %>");
  break;
  case "/followers":
  $(".jscroll-inner").html("<%= j(render partial: "users/users", locals: {users: @referer_user.followers.order('relationships.created_at DESC')}) %>");
  break;
}
$(".profile").html("<%= j(render partial: "users/profile", locals: {user: @referer_user}) %>");
