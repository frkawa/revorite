= render "shared/header"

.main
  .edituser
    .edituser-header
      プロフィールの編集
    .edituser-items
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, novalidate: true }) do |f|
        = render "devise/shared/error_messages", resource: resource
        .edituser-items__label
          = f.label :name
          %span.note （30文字以内）
        .edituser-items__field
          = f.text_field :name, autocomplete: "name", id: "useredit-input-name"
        .edituser-items__label
          = f.label :description
          %span.note （150文字以内）
        .edituser-items__field
          = f.text_area :description, autocomplete: "description"
        .edituser-items__label
          = f.label :image
        .edituser-items__field
          = f.file_field :image, autocomplete: "image"
        -# .field
        -#   = f.label :email
        -#   %br/
        -#   = f.email_field :email, autocomplete: "email"
        -# - if devise_mapping.confirmable? && resource.pending_reconfirmation?
        -#   %div
        -#     Currently waiting confirmation for: #{resource.unconfirmed_email}
        -# .field
        -#   = f.label :password
        -#   %i (leave blank if you don't want to change it)
        -#   %br/
        -#   = f.password_field :password, autocomplete: "new-password"
        -#   - if @minimum_password_length
        -#     %br/
        -#     %em
        -#       = @minimum_password_length
        -#       characters minimum
        -# .field
        -#   = f.label :password_confirmation
        -#   %br/
        -#   = f.password_field :password_confirmation, autocomplete: "new-password"
        -# .field
        -#   = f.label :current_password
        -#   %i (we need your current password to confirm your changes)
        -#   %br/
        -#   = f.password_field :current_password, autocomplete: "current-password"
        .edituser-items__submit
          = f.submit "更新する", class: "button-submit user-edit", id: "useredit-button-submit"#, disabled: true
      %h3 Cancel my account
      %p
        Unhappy? #{button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete}
    .edituser-footer
      = link_to("<< トップに戻る", root_path)

= render "shared/footer"